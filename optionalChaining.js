// optional Chaining

const user = {
    firstName : 'Shaurya',
    // address : {house_no : '92203'}
}

console.log(user?.firstName);
console.log(user?.address?.house_no);

// optional Chaining it didn't return error 

